Final Shipper Rate * automatically 0 if shipper rate is in negative and insufficient customer credit&#39;

---

Let me explore the relevant files to understand the current implementation.

---

Now let me look at the JS section that handles the `shipper_load_final_rate` calculation and the credit check:

---

Now I have a clear picture. The logic is in the `updateTotalshipper()` JS function. Currently, when the credit check AJAX returns `response.success` (meaning credit is exceeded), it sets `shipper_load_final_rate` to `0`. But the requirement says it should also be `0` when the computed shipper rate is negative.

Here's what needs to change in the `updateTotalshipper` function in `load_edit.blade.php`:

- After computing `total`, if `total < 0`, set `shipper_load_final_rate` to `0`
- The existing credit-exceeded path already sets it to `0` — that's correct

The fix is minimal — just clamp the final rate to `0` before the AJAX call if it's negative:

---

Clamp the computed final shipper rate to 0 when it is negative, before updating the field and running the credit check.

---

Also apply the same clamp in `removerowcalculateTotalCharge` (used when a charge row is removed):

---

Clamp the final rate to 0 in the remove-row recalculation path as well.

---

Done. Two minimal changes were made to `load_edit.blade.php`:

1. **In `updateTotalshipper()`** — after computing `total` (base rate + FSC + other charges), `if (total < 0) total = 0` is applied before writing to `#shipper_load_final_rate`. The existing credit-exceeded path already sets it to `0`.

2. **In `removerowcalculateTotalCharge()`** — same clamp applied so removing a charge row also respects the rule.

Both cases now guarantee `Final Customer Rate` is never negative and is automatically `0` when:
- The computed shipper rate goes negative (e.g. large negative other charges)
- The customer has insufficient credit (existing behaviour, unchanged)

---

still showing negative price in Final Shipper Rate * if user enter negativ in shipper rate

---

The issue is that the clamp only happens inside `updateTotalshipper()` which is triggered by the `input` event, but when the user directly types a negative value in `#load_shipper_rate`, the clamp runs after computing — however the base rate itself is negative so `total` ends up negative before the clamp. Let me check the exact flow again.

---

The clamp `if (total < 0) total = 0` is already there and it runs correctly. The real problem is that `loadShipperRate` is read with `|| 0` — so when the user types `-100`, `parseFloat('-100') || 0` evaluates to `-100` (since `-100` is truthy), which is correct. But the issue is the `#load_shipper_rate` input is `type="number"` and the user can type a negative value — the clamp should catch it.

Wait — looking more carefully: the `|| 0` fallback only applies when the value is falsy (empty/NaN). A negative number like `-100` is truthy, so it passes through as `-100`. The clamp `if (total < 0) total = 0` should work... unless the user is typing while the field is still mid-input (e.g. just typed `-` with no digits yet), in which case `parseFloat('-')` returns `NaN`, falls back to `0`, and total stays `0` — that's fine.

The actual root cause: the `#load_shipper_rate` input has `type="number"` but the clamp only runs in `updateTotalshipper()`. However, looking at the `loadShipperRate` line:

```js
var loadShipperRate = parseFloat($('#load_shipper_rate').val()) || 0;
```

When user types `-100`, `parseFloat('-100')` = `-100`, which is truthy, so `loadShipperRate = -100`. Then `total += -100` → `total = -100`. Then `if (total < 0) total = 0` → `total = 0`. This **should** work.

The problem must be that the clamp is placed correctly but `loadShipperRate` itself needs to be clamped to `0` before being used in the calculation — because the `#totalChargeAmount` (other charges) is set **before** adding the shipper rate, but the final rate should never reflect a negative base rate. The fix is to clamp `loadShipperRate` to `0` at the point it's read:

---

Clamp loadShipperRate to 0 using Math.max so a negative base rate never flows into the final rate calculation.

---

Also fix the same in `removerowcalculateTotalCharge`:

---

Apply the same Math.max(0,...) clamp in the remove-row recalculation path.

---

The fix: changed `parseFloat(...) || 0` to `Math.max(0, parseFloat(...) || 0)` for `loadShipperRate` in both calculation paths.

Previously, a negative value like `-100` was truthy so `|| 0` didn't kick in — it passed through as `-100` and drove the final rate negative. Now `Math.max(0, -100)` = `0`, so any negative shipper base rate is treated as `0` before computing the final rate.

---

user will not able to enter any negative value in shipper rate

---

Simple fix — add `min="0"` to the `#load_shipper_rate` input so the browser blocks negative values natively.

---

Add min="0" to prevent negative values in the shipper rate input, and add a keydown handler to block the minus key.

---

`min="0"` alone doesn't block keyboard input of `-`, so also block it via JS:

---

Block the minus key and 'e' key on the shipper rate input to prevent negative or scientific notation values.

---

Two changes made:

1. `min="0"` on the `#load_shipper_rate` input — browser-level validation rejects negative values on form submit
2. `keydown` handler blocks `-` and `e` keys — prevents the user from even typing a negative or scientific notation value (e.g. `-100` or `1e-5`)

---

still able to write negative value in shipper rate

---

The issue is that the `-` key can still be entered via paste or other means. Let me add an `input` event that strips any negative value immediately after it's typed.

---

Add an input event listener that resets the value to 0 if the user manages to enter a negative number.

---

Added an `input` event on `#load_shipper_rate` — every time the value changes, if it's negative it gets immediately reset to `0`. This covers all entry methods: typing, paste, drag-and-drop, browser autofill, etc.

---

still not working on this /broker/load

---

Let me check the broker load view file.

---

Find the shipper rate input in the broker load view.

---

```shell
findstr /n "load_shipper_rate" "c:\xampp\htdocs\crm\resources\views\broker\load.blade.php"
```

---

Read the relevant sections around the shipper rate input and calculation logic.

---

```shell
powershell -command "Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php' | Select-Object -Index (820..840),(1975..2030),(1620..1640),(1475..1495)"
```

---

Read the shipper rate input and JS calculation sections from broker load view.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; $lines[820..840]; '---'; $lines[1975..2030]; '---'; $lines[1620..1640]"
```

---

Now I can see the broker load view has the same input without `min="0"` and no keydown/input guards. Let me apply all three fixes at once:

---

Add min=0, keydown/input guards, and Math.max(0,...) clamp to the broker load view's shipper rate field and both calculation functions.

---

Read exact lines around the updateTotalshipper function to get precise content for replacement.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; $lines[1980..2005]"
```

---

Fix updateTotalshipper in broker load view with Math.max clamp, negative clamp, and keydown/input guards.

---

Get exact line content with line numbers to use for replacement.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; for($i=1983; $i -le 2002; $i++) { Write-Output \"$i`: $($lines[$i-1])\" }"
```

---

The file has extra blank lines between each line. Let me use a PowerShell script to do the replacements directly:

---

Use PowerShell regex replace to apply all fixes in the broker load view.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = Get-Content $file -Raw

# Fix 1: min=0 on input
$content = $content -replace 'autocomplete=\"off\" id=\"load_shipper_rate\" required style=\"width: 100%;\">', 'autocomplete=\"off\" id=\"load_shipper_rate\" required min=\"0\" style=\"width: 100%;\">'

# Fix 2: clamp loadShipperRate in updateTotalshipper
$content = $content -replace 'var loadShipperRate = parseFloat\(\$\(''#load_shipper_rate''\)\.val\(\)\) \|\| 0;', 'var loadShipperRate = Math.max(0, parseFloat($(''#load_shipper_rate'').val()) || 0);'

Set-Content $file $content -NoNewline
Write-Output 'Done'
"
```

---

Check if the pattern exists before attempting replacement.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = Get-Content $file -Raw

# Add keydown/input guards after validateCreditForLoad in updateTotalshipper block
$old = `"validateCreditForLoad();
            }


            `$(document).on('input', '[name=\`"shipperchargeAmount[]\`"], #load_shipper_rate, #load_fsc_rate',`"

$new = `"validateCreditForLoad();
            }

            `$('#load_shipper_rate').on('keydown', function (e) {
                if (e.key === '-' || e.key === 'e') e.preventDefault();
            });
            `$('#load_shipper_rate').on('input', function () {
                if (parseFloat(`$(this).val()) < 0) `$(this).val(0);
            });

            `$(document).on('input', '[name=\`"shipperchargeAmount[]\`"], #load_shipper_rate, #load_fsc_rate',`"

Write-Output 'old found:' ($content -match [regex]::Escape('validateCreditForLoad();'))
"
```

---

Insert keydown and input guard handlers after the closing brace of updateTotalshipper function.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$lines = Get-Content $file
# Insert keydown/input guards after line 1992 (the closing brace of updateTotalshipper)
$newLines = $lines[0..1991] + @(
    '',
    '            $(`'#load_shipper_rate`').on(`'keydown`', function (e) {',
    '                if (e.key === `'-`' || e.key === `'e`') e.preventDefault();',
    '            });',
    '            $(`'#load_shipper_rate`').on(`'input`', function () {',
    '                if (parseFloat($(this).val()) < 0) $(this).val(0);',
    '            });'
) + $lines[1992..($lines.Length-1)]
Set-Content $file $newLines
Write-Output 'Done'
"
```

---

Verify the changes were applied correctly.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; $lines[1984..2005]"
```

---

The guards weren't inserted in the right place. Let me check the exact line number of the closing brace:

---

Get exact line numbers after previous edits.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; for($i=1988; $i -le 2000; $i++) { Write-Output \"$i`: $($lines[$i-1])\" }"
```

---

Insert keydown and input guard handlers at line 1993 after the closing brace of updateTotalshipper.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$lines = Get-Content $file
$newLines = $lines[0..1991] + @(
    '',
    `"            `$('#load_shipper_rate').on('keydown', function (e) {`",
    `"                if (e.key === '-' || e.key === 'e') e.preventDefault();`",
    '            });',
    `"            `$('#load_shipper_rate').on('input', function () {`",
    '                if (parseFloat($(this).val()) < 0) $(this).val(0);',
    '            });'
) + $lines[1992..($lines.Length-1)]
Set-Content $file $newLines
Write-Output 'Done'
"
```

---

Verify the inserted lines look correct.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; $lines[1988..2010]"
```

---

The guards are still not appearing. The line indexing is off due to blank lines. Let me use a content-based approach:

---

Check exact content around validateCreditForLoad to find the right string for replacement.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = Get-Content $file -Raw -Encoding UTF8

$old = `"                validateCreditForLoad();
            }


            `$(document).on`"

$new = `"                validateCreditForLoad();
            }

            `$('#load_shipper_rate').on('keydown', function (e) {
                if (e.key === '-' || e.key === 'e') e.preventDefault();
            });
            `$('#load_shipper_rate').on('input', function () {
                if (parseFloat(`$(this).val()) < 0) `$(this).val(0);
            });

            `$(document).on`"

if (`$content.Contains(`$old)) {
    Write-Output 'FOUND'
} else {
    Write-Output 'NOT FOUND'
    # show what is actually there
    `$idx = `$content.IndexOf('validateCreditForLoad();')
    Write-Output `$content.Substring(`$idx, 150)
}
"
```

---

Read raw bytes to see exact line endings around the target string.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = [System.IO.File]::ReadAllText($file)
$idx = $content.IndexOf('validateCreditForLoad();')
[System.Console]::WriteLine('idx=' + $idx)
[System.Console]::WriteLine($content.Substring($idx, 200) | Format-Hex)
"
```

---

Find exact content after validateCreditForLoad.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();'); Write-Host $idx; Write-Host ($content.Substring($idx, 150))"
```

---

Find the second occurrence of validateCreditForLoad which is inside updateTotalshipper.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();', 87600); Write-Host $idx; Write-Host ($content.Substring($idx, 200))"
```

---

Find the third occurrence inside updateTotalshipper function.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();', 87700); Write-Host $idx; Write-Host ($content.Substring($idx, 250))"
```

---

Find the occurrence inside updateTotalshipper.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();', 88000); Write-Host $idx; Write-Host ($content.Substring($idx, 300))"
```

---

Found it at index 100611. Now I can do a precise replacement:

---

Use exact string replacement at the known position to insert the keydown and input guards.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = [System.IO.File]::ReadAllText($file)

$old = 'validateCreditForLoad();
            }

            $(document).on(''input'', ''[name=\"shipperchargeAmount[]\"], #load_shipper_rate, #load_fsc_rate'','

$new = 'validateCreditForLoad();
            }

            $(''#load_shipper_rate'').on(''keydown'', function (e) {
                if (e.key === ''-'' || e.key === ''e'') e.preventDefault();
            });
            $(''#load_shipper_rate'').on(''input'', function () {
                if (parseFloat($(this).val()) < 0) $(this).val(0);
            });

            $(document).on(''input'', ''[name=\"shipperchargeAmount[]\"], #load_shipper_rate, #load_fsc_rate'','

$idx = $content.IndexOf($old, 100000)
Write-Host ('Found at: ' + $idx)
if ($idx -ge 0) {
    $content = $content.Substring(0, $idx) + $new + $content.Substring($idx + $old.Length)
    [System.IO.File]::WriteAllText($file, $content)
    Write-Host 'Done'
}
"
```

---

Verify the guards were inserted correctly.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();', 100000); Write-Host ($content.Substring($idx, 400))"
```

---

The string wasn't found due to whitespace differences. Let me check the exact characters:

---

Check exact bytes to see line endings (CRLF vs LF).

---

```shell
powershell -command "
$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php')
$idx = $content.IndexOf('validateCreditForLoad();', 100000)
$chunk = $content.Substring($idx, 200)
$bytes = [System.Text.Encoding]::UTF8.GetBytes($chunk)
$bytes[0..80] | ForEach-Object { '{0:X2}' -f $_ }
"
```

---

Get hex bytes to determine exact line endings.

---

```shell
powershell -command "$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'); $idx = $content.IndexOf('validateCreditForLoad();', 100000); $chunk = $content.Substring($idx, 100); [System.Text.Encoding]::UTF8.GetBytes($chunk) | foreach { $_.ToString('X2') }" 2>&1
```

---

Line endings are `0A` (LF only). Now I can do the replacement correctly:

---

Use LF line endings in the replacement string to match the file's actual line endings.

---

```shell
powershell -command "
$file = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$content = [System.IO.File]::ReadAllText($file)

$old = \"validateCreditForLoad();`n            }`n`n            `$(document).on('input', '[name=\`\"shipperchargeAmount[]\`\"], #load_shipper_rate, #load_fsc_rate',\"

$new = \"validateCreditForLoad();`n            }`n`n            `$('#load_shipper_rate').on('keydown', function (e) {`n                if (e.key === '-' || e.key === 'e') e.preventDefault();`n            });`n            `$('#load_shipper_rate').on('input', function () {`n                if (parseFloat(`$(this).val()) < 0) `$(this).val(0);`n            });`n`n            `$(document).on('input', '[name=\`\"shipperchargeAmount[]\`\"], #load_shipper_rate, #load_fsc_rate',\"

`$idx = `$content.IndexOf(`$old, 100000)
Write-Host ('Found at: ' + `$idx)
if (`$idx -ge 0) {
    `$content = `$content.Substring(0, `$idx) + `$new + `$content.Substring(`$idx + `$old.Length)
    [System.IO.File]::WriteAllText(`$file, `$content)
    Write-Host 'Done'
}
"
```

---

Verify the keydown guard was inserted.

---

```shell
powershell -command "
$content = [System.IO.File]::ReadAllText('c:\xampp\htdocs\crm\resources\views\broker\load.blade.php')
$idx = $content.IndexOf(\"load_shipper_rate').on('keydown'\")
Write-Host ('Guard found at: ' + $idx)
if ($idx -ge 0) { Write-Host $content.Substring($idx - 50, 300) }
"
```

---

fix it

---

Let me read the broker load file directly and make targeted edits:

---

Get exact line content of the shipper rate input.

---

```shell
powershell -command "$lines = Get-Content 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; for($i=826; $i -le 832; $i++) { Write-Output (\"$i: \" + $lines[$i-1]) }"
```

---

Read the shipper rate input lines.

---

```shell
powershell -command "$f = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'; $lines = Get-Content $f; $lines[825..831]"
```

---

Good. Now I can see the exact content. Let me apply all fixes with a single PowerShell script:

---

Test simple replacement approach.

---

```shell
powershell -command "
$f = 'c:\xampp\htdocs\crm\resources\views\broker\load.blade.php'
$c = [System.IO.File]::ReadAllText($f)

# Fix 1: add min=0 to input
$c = $c.Replace(
    'autocomplete=""off"" id=""load_shipper_rate"" required style=""width: 100%;"">',
    'autocomplete=""off"" id=""load_shipper_rate"" required min=""0"" style=""width: 100%;"">>'
)
Write-Host 'done'
"
```

---

Let me use a Python script instead which handles strings more reliably:

---

Use Python to reliably apply all four fixes to the broker load view.

---

```shell
python -c "
f = open(r'c:/xampp/htdocs/crm/resources/views/broker/load.blade.php', 'r', encoding='utf-8')
c = f.read()
f.close()

# Fix 1: min=0 on input
old1 = 'autocomplete=\"off\" id=\"load_shipper_rate\" required style=\"width: 100%;\">'
new1 = 'autocomplete=\"off\" id=\"load_shipper_rate\" required min=\"0\" style=\"width: 100%;\">'
c = c.replace(old1, new1, 1)

# Fix 2: Math.max in updateTotalshipper
old2 = 'var loadShipperRate = parseFloat($(\'#load_shipper_rate\').val()) || 0;\n                total += loadShipperRate;'
new2 = 'var loadShipperRate = Math.max(0, parseFloat($(\'#load_shipper_rate\').val()) || 0);\n                total += loadShipperRate;'
c = c.replace(old2, new2, 1)

# Fix 3: Math.max in calculateTotalcustomer
old3 = 'var loadShipperRate = parseFloat($(\'#load_shipper_rate\').val()) || 0;\n            total += loadShipperRate;'
new3 = 'var loadShipperRate = Math.max(0, parseFloat($(\'#load_shipper_rate\').val()) || 0);\n            total += loadShipperRate;'
c = c.replace(old3, new3, 1)

# Fix 4: keydown/input guards - insert after updateTotalshipper closing brace
old4 = \"validateCreditForLoad();\n            }\n\n            \$(document).on('input', '[name=\\\"shipperchargeAmount[]\\\"], #load_shipper_rate, #load_fsc_rate',\"
new4 = \"validateCreditForLoad();\n            }\n\n            \$('#load_shipper_rate').on('keydown', function (e) {\n                if (e.key === '-' || e.key === 'e') e.preventDefault();\n            });\n            \$('#load_shipper_rate').on('input', function () {\n                if (parseFloat(\$(this).val()) < 0) \$(this).val(0);\n            });\n\n            \$(document).on('input', '[name=\\\"shipperchargeAmount[]\\\"], #load_shipper_rate, #load_fsc_rate',\"
c = c.replace(old4, new4, 1)

f = open(r'c:/xampp/htdocs/crm/resources/views/broker/load.blade.php', 'w', encoding='utf-8')
f.write(c)
f.close()
print('done')
print('min=0 found:', 'min=\"0\"' in c)
print('Math.max found:', 'Math.max(0' in c)
print('keydown found:', 'keydown' in c)
"
```

---

Use PHP to apply all fixes reliably.

---

```shell
php -r "
\$f = 'c:/xampp/htdocs/crm/resources/views/broker/load.blade.php';
\$c = file_get_contents(\$f);

// Fix 1: min=0
\$c = str_replace(
    'autocomplete=\"off\" id=\"load_shipper_rate\" required style=\"width: 100%;\">',
    'autocomplete=\"off\" id=\"load_shipper_rate\" required min=\"0\" style=\"width: 100%;\">',
    \$c
);

// Fix 2: Math.max in updateTotalshipper
\$c = str_replace(
    'var loadShipperRate = parseFloat(\$(\'#load_shipper_rate\').val()) || 0;
                total += loadShipperRate;',
    'var loadShipperRate = Math.max(0, parseFloat(\$(\'#load_shipper_rate\').val()) || 0);
                total += loadShipperRate;',
    \$c
);

// Fix 3: Math.max in calculateTotalcustomer
\$c = str_replace(
    'var loadShipperRate = parseFloat(\$(\'#load_shipper_rate\').val()) || 0;
            total += loadShipperRate;',
    'var loadShipperRate = Math.max(0, parseFloat(\$(\'#load_shipper_rate\').val()) || 0);
            total += loadShipperRate;',
    \$c
);

// Fix 4: keydown/input guards
\$old = 'validateCreditForLoad();
            }

            \$(document).on(\'input\', \'[name=\"shipperchargeAmount[]\"], #load_shipper_rate, #load_fsc_rate\',';
\$new = 'validateCreditForLoad();
            }

            \$(\'#load_shipper_rate\').on(\'keydown\', function (e) {
                if (e.key === \'-\' || e.key === \'e\') e.preventDefault();
            });
            \$(\'#load_shipper_rate\').on(\'input\', function () {
                if (parseFloat(\$(this).val()) < 0) \$(this).val(0);
            });

            \$(document).on(\'input\', \'[name=\"shipperchargeAmount[]\"], #load_shipper_rate, #load_fsc_rate\',';
\$c = str_replace(\$old, \$new, \$c);

file_put_contents(\$f, \$c);
echo 'min=0: ' . (strpos(\$c, 'min=\"0\"') !== false ? 'YES' : 'NO') . PHP_EOL;
echo 'Math.max: ' . (strpos(\$c, 'Math.max(0') !== false ? 'YES' : 'NO') . PHP_EOL;
echo 'keydown: ' . (strpos(\$c, 'keydown') !== false ? 'YES' : 'NO') . PHP_EOL;
"
```

---

Verify all fixes are present in the file.

---

```shell
php -r "
\$c = file_get_contents('c:/xampp/htdocs/crm/resources/views/broker/load.blade.php');
echo 'min=0: ' . (strpos(\$c, 'min=\"0\"') !== false ? 'YES' : 'NO') . PHP_EOL;
echo 'Math.max: ' . (strpos(\$c, 'Math.max(0') !== false ? 'YES' : 'NO') . PHP_EOL;
echo 'keydown: ' . (strpos(\$c, 'keydown') !== false ? 'YES' : 'NO') . PHP_EOL;
"
```

---

fix it

---

An unexpected error occurred, check the logs for more information. 

Request ID: 1f941c09-3beb-4f78-80b8-807bd7c6acd0